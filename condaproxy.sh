# for if you ever wish to use proxyservers, some commands that might be helpful.
# I had less success with proxyserver, so just prefer using the Wifi stick.


conda config --set proxy_servers.http http://id:pw@address:port
conda config --set proxy_servers.https https://id:pw@address:port
